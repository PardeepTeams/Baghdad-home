<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected dot -->
    <item android:state_selected="true">
        <shape android:shape="oval">
            <size
                android:width="8dp"
                android:height="8dp" />
            <solid android:color="@color/blue" />
            <padding
                android:left="6dp"
                android:top="6dp"
                android:bottom="6dp"
                android:right="6dp" /> <!-- gap between dots -->
        </shape>
    </item>

    <!-- Unselected dot -->
    <item>
        <shape android:shape="oval">
            <size
                android:width="6dp"
                android:height="6dp" />
            <solid android:color="@color/lightBlue" />
            <padding
                android:left="6dp"
                android:top="6dp"
                android:bottom="6dp"
                android:right="6dp"/> <!-- gap between dots -->
        </shape>
    </item>
</selector>
